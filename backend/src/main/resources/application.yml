spring:
  datasource:
    url: jdbc:mysql://mysql:3306/rag?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: redis
    port: 6379

milvus:
  host: milvus
  port: 19530
  collection-name: rag_collection
  embedding-dimension: 1536

minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: rag-bucket

openai:
  api-key: dummy

server:
  port: 8080